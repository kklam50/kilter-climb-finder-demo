# Only needed to run scripts/export_static_demo.py / verify_static_demo.py.
# The deployed demo is static files; sentence-transformers is stubbed out
# by the scripts (the model is never used at search time).
numpy
rapidfuzz
